const axios = require('axios');

async function test() {
  try {
    // Assuming we have an announcement ID from the screenshot or typical data
    // I'll try to find one by first getting the workspace announcements
    const workspaceId = '77841e2b-c687-4230-aec5-cb983be33110'; // From user's previous terminal log
    
    // We need a token. I'll skip this for now and just check the code more carefully.
    // Actually, I can't easily test without a token.
  } catch (e) {
    console.error(e);
  }
}
